/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { PANDA_BATTLE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const REGENERATE_TURNS_DISCRIMINATOR = new Uint8Array([
  180, 10, 32, 114, 161, 116, 35, 58,
]);

export function getRegenerateTurnsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGENERATE_TURNS_DISCRIMINATOR
  );
}

export type RegenerateTurnsInstruction<
  TProgram extends string = typeof PANDA_BATTLE_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountGameConfig extends string | AccountMeta<string> = string,
  TAccountGameRound extends string | AccountMeta<string> = string,
  TAccountPlayerState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountGameConfig extends string
        ? ReadonlyAccount<TAccountGameConfig>
        : TAccountGameConfig,
      TAccountGameRound extends string
        ? ReadonlyAccount<TAccountGameRound>
        : TAccountGameRound,
      TAccountPlayerState extends string
        ? WritableAccount<TAccountPlayerState>
        : TAccountPlayerState,
      ...TRemainingAccounts,
    ]
  >;

export type RegenerateTurnsInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RegenerateTurnsInstructionDataArgs = {};

export function getRegenerateTurnsInstructionDataEncoder(): FixedSizeEncoder<RegenerateTurnsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REGENERATE_TURNS_DISCRIMINATOR })
  );
}

export function getRegenerateTurnsInstructionDataDecoder(): FixedSizeDecoder<RegenerateTurnsInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRegenerateTurnsInstructionDataCodec(): FixedSizeCodec<
  RegenerateTurnsInstructionDataArgs,
  RegenerateTurnsInstructionData
> {
  return combineCodec(
    getRegenerateTurnsInstructionDataEncoder(),
    getRegenerateTurnsInstructionDataDecoder()
  );
}

export type RegenerateTurnsAsyncInput<
  TAccountCaller extends string = string,
  TAccountGameConfig extends string = string,
  TAccountGameRound extends string = string,
  TAccountPlayerState extends string = string,
> = {
  /** Anyone can call this (crank) */
  caller: TransactionSigner<TAccountCaller>;
  gameConfig?: Address<TAccountGameConfig>;
  gameRound: Address<TAccountGameRound>;
  playerState: Address<TAccountPlayerState>;
};

export async function getRegenerateTurnsInstructionAsync<
  TAccountCaller extends string,
  TAccountGameConfig extends string,
  TAccountGameRound extends string,
  TAccountPlayerState extends string,
  TProgramAddress extends Address = typeof PANDA_BATTLE_PROGRAM_ADDRESS,
>(
  input: RegenerateTurnsAsyncInput<
    TAccountCaller,
    TAccountGameConfig,
    TAccountGameRound,
    TAccountPlayerState
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  RegenerateTurnsInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountGameConfig,
    TAccountGameRound,
    TAccountPlayerState
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PANDA_BATTLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    gameConfig: { value: input.gameConfig ?? null, isWritable: false },
    gameRound: { value: input.gameRound ?? null, isWritable: false },
    playerState: { value: input.playerState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.gameConfig.value) {
    accounts.gameConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([103, 97, 109, 101, 95, 99, 111, 110, 102, 105, 103])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.gameConfig),
      getAccountMeta(accounts.gameRound),
      getAccountMeta(accounts.playerState),
    ],
    data: getRegenerateTurnsInstructionDataEncoder().encode({}),
    programAddress,
  } as RegenerateTurnsInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountGameConfig,
    TAccountGameRound,
    TAccountPlayerState
  >);
}

export type RegenerateTurnsInput<
  TAccountCaller extends string = string,
  TAccountGameConfig extends string = string,
  TAccountGameRound extends string = string,
  TAccountPlayerState extends string = string,
> = {
  /** Anyone can call this (crank) */
  caller: TransactionSigner<TAccountCaller>;
  gameConfig: Address<TAccountGameConfig>;
  gameRound: Address<TAccountGameRound>;
  playerState: Address<TAccountPlayerState>;
};

export function getRegenerateTurnsInstruction<
  TAccountCaller extends string,
  TAccountGameConfig extends string,
  TAccountGameRound extends string,
  TAccountPlayerState extends string,
  TProgramAddress extends Address = typeof PANDA_BATTLE_PROGRAM_ADDRESS,
>(
  input: RegenerateTurnsInput<
    TAccountCaller,
    TAccountGameConfig,
    TAccountGameRound,
    TAccountPlayerState
  >,
  config?: { programAddress?: TProgramAddress }
): RegenerateTurnsInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountGameConfig,
  TAccountGameRound,
  TAccountPlayerState
> {
  // Program address.
  const programAddress = config?.programAddress ?? PANDA_BATTLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    gameConfig: { value: input.gameConfig ?? null, isWritable: false },
    gameRound: { value: input.gameRound ?? null, isWritable: false },
    playerState: { value: input.playerState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.gameConfig),
      getAccountMeta(accounts.gameRound),
      getAccountMeta(accounts.playerState),
    ],
    data: getRegenerateTurnsInstructionDataEncoder().encode({}),
    programAddress,
  } as RegenerateTurnsInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountGameConfig,
    TAccountGameRound,
    TAccountPlayerState
  >);
}

export type ParsedRegenerateTurnsInstruction<
  TProgram extends string = typeof PANDA_BATTLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Anyone can call this (crank) */
    caller: TAccountMetas[0];
    gameConfig: TAccountMetas[1];
    gameRound: TAccountMetas[2];
    playerState: TAccountMetas[3];
  };
  data: RegenerateTurnsInstructionData;
};

export function parseRegenerateTurnsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedRegenerateTurnsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      gameConfig: getNextAccount(),
      gameRound: getNextAccount(),
      playerState: getNextAccount(),
    },
    data: getRegenerateTurnsInstructionDataDecoder().decode(instruction.data),
  };
}
