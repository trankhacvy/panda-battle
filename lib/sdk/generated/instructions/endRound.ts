/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { PANDA_BATTLE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const END_ROUND_DISCRIMINATOR = new Uint8Array([
  54, 47, 1, 200, 250, 6, 144, 63,
]);

export function getEndRoundDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(END_ROUND_DISCRIMINATOR);
}

export type EndRoundInstruction<
  TProgram extends string = typeof PANDA_BATTLE_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountGameConfig extends string | AccountMeta<string> = string,
  TAccountGameRound extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountGameConfig extends string
        ? ReadonlyAccount<TAccountGameConfig>
        : TAccountGameConfig,
      TAccountGameRound extends string
        ? WritableAccount<TAccountGameRound>
        : TAccountGameRound,
      ...TRemainingAccounts,
    ]
  >;

export type EndRoundInstructionData = { discriminator: ReadonlyUint8Array };

export type EndRoundInstructionDataArgs = {};

export function getEndRoundInstructionDataEncoder(): FixedSizeEncoder<EndRoundInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: END_ROUND_DISCRIMINATOR })
  );
}

export function getEndRoundInstructionDataDecoder(): FixedSizeDecoder<EndRoundInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getEndRoundInstructionDataCodec(): FixedSizeCodec<
  EndRoundInstructionDataArgs,
  EndRoundInstructionData
> {
  return combineCodec(
    getEndRoundInstructionDataEncoder(),
    getEndRoundInstructionDataDecoder()
  );
}

export type EndRoundAsyncInput<
  TAccountAdmin extends string = string,
  TAccountGameConfig extends string = string,
  TAccountGameRound extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  gameConfig?: Address<TAccountGameConfig>;
  gameRound: Address<TAccountGameRound>;
};

export async function getEndRoundInstructionAsync<
  TAccountAdmin extends string,
  TAccountGameConfig extends string,
  TAccountGameRound extends string,
  TProgramAddress extends Address = typeof PANDA_BATTLE_PROGRAM_ADDRESS,
>(
  input: EndRoundAsyncInput<
    TAccountAdmin,
    TAccountGameConfig,
    TAccountGameRound
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  EndRoundInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountGameConfig,
    TAccountGameRound
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PANDA_BATTLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    gameConfig: { value: input.gameConfig ?? null, isWritable: false },
    gameRound: { value: input.gameRound ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.gameConfig.value) {
    accounts.gameConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([103, 97, 109, 101, 95, 99, 111, 110, 102, 105, 103])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.gameConfig),
      getAccountMeta(accounts.gameRound),
    ],
    data: getEndRoundInstructionDataEncoder().encode({}),
    programAddress,
  } as EndRoundInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountGameConfig,
    TAccountGameRound
  >);
}

export type EndRoundInput<
  TAccountAdmin extends string = string,
  TAccountGameConfig extends string = string,
  TAccountGameRound extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  gameConfig: Address<TAccountGameConfig>;
  gameRound: Address<TAccountGameRound>;
};

export function getEndRoundInstruction<
  TAccountAdmin extends string,
  TAccountGameConfig extends string,
  TAccountGameRound extends string,
  TProgramAddress extends Address = typeof PANDA_BATTLE_PROGRAM_ADDRESS,
>(
  input: EndRoundInput<TAccountAdmin, TAccountGameConfig, TAccountGameRound>,
  config?: { programAddress?: TProgramAddress }
): EndRoundInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountGameConfig,
  TAccountGameRound
> {
  // Program address.
  const programAddress = config?.programAddress ?? PANDA_BATTLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    gameConfig: { value: input.gameConfig ?? null, isWritable: false },
    gameRound: { value: input.gameRound ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.gameConfig),
      getAccountMeta(accounts.gameRound),
    ],
    data: getEndRoundInstructionDataEncoder().encode({}),
    programAddress,
  } as EndRoundInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountGameConfig,
    TAccountGameRound
  >);
}

export type ParsedEndRoundInstruction<
  TProgram extends string = typeof PANDA_BATTLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    gameConfig: TAccountMetas[1];
    gameRound: TAccountMetas[2];
  };
  data: EndRoundInstructionData;
};

export function parseEndRoundInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedEndRoundInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      gameConfig: getNextAccount(),
      gameRound: getNextAccount(),
    },
    data: getEndRoundInstructionDataDecoder().decode(instruction.data),
  };
}
